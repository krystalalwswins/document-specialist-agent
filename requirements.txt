# === Runtime ===
# AIO Sandbox Python SDK (official: Python 3.8+, local: 3.13.2; latest on PyPI is 0.0.30)
agent-sandbox==0.0.30
# Object storage (MinIO / S3-compatible)
boto3>=1.34.0
# LLM client (OpenAI-compatible, e.g. DeepSeek)
openai>=1.12.0
# .env loading
python-dotenv>=1.0.0
# Typed settings (Phase 1 Step 2: core/config.py)
pydantic-settings>=2.0.0
# API layer (Phase 1 Step 7)
fastapi>=0.110.0
uvicorn>=0.29.0
jsonschema>=4.18,<5

# === Dev ===
pytest>=8.0.0

# Local CSV/XLSX parsing (MCP remains optional)
openpyxl>=3.1,<4
